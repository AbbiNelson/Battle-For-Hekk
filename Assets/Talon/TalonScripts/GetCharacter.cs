using UnityEngine;

public class GetCharacter : MonoBehaviour
{
    public void LoadSceneAndKeepValue()
    {
        // Use this to save info, have to wait until we add multiple controller support in ChrSelect
        //string P1 = player 1's character choice
        //Then use if (All the players selected a character) then load the next scene
    }
}
